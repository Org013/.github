name: "Mirror .github"

on:
  push: 
    branches:
      - "main"
      - "dev"

jobs:
  mirror:
    uses: org013/.github/workflows/mirrors.yml@main
    with:
      codeberg-repo: https://codeberg.org/Org013/.codeberg
