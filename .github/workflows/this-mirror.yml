name: "Mirror .github"

on:
  push: 
    branches:
      - "main"
      - "dev"

jobs:
  mirror:
    uses: ./.github/workflows/mirrors.yml
    with:
      codeberg-repo: https://codeberg.org/Org013/.codeberg
